[server]
host = "0.0.0.0"
port = 3000

[database]
path = "data/db/harvex.duckdb"

[storage]
upload_dir = "data/uploads"
max_file_size_mb = 50

[processing]
max_concurrent = 2

[llm]
# OpenAI-compatible API endpoint (Ollama, llama.cpp server, vLLM, cloud)
api_url = "http://localhost:11434/v1"
api_key = ""
model_name = "qwen2.5:7b"
context_size = 4096
temperature = 0.1
max_tokens = 2048
# Vision model for scanned PDFs and images (empty = disabled)
vision_model_name = ""
vision_dpi = 200
vision_max_pages = 5
